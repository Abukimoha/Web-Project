
@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<section class="vh-100">
    <div class="container py-5 h-100">
        <div class="row d-flex align-items-center justify-content-center h-100">
        </div>
    </div>
    <form>
        <li>
            @Html.ActionLink("User", "Login", "User", null, new { @class = "btn btn-primary btn-lg btn-block" })
        </li>
        <li>
            @Html.ActionLink("Admin", "Login", "Admin", null, new { @class = "btn btn-primary btn-lg btn-block" })
        </li>
    </form>
</section>
